// Domains excluded from page fetching (enrichResults).
// The canonical list lives in the DB (blocked_domains table) and is managed dynamically.
// Add entries here only as a last resort static seed — prefer letting the auto-block system learn.
const BLOCKED_DOMAINS = [];
